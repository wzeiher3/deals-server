DROP TABLE IF EXISTS deals_table;

CREATE TABLE deals_table (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    name TEXT NOT NULL,
    price INTEGER NOT NULL,
    distance INTEGER NOT NULL,
    day TEXT NOT NULL,
    content TEXT
);